/*
 * Performance benchmarks temporarily disabled due to compilation issues
 * TODO: Fix benchmark code to work with current API structure
 */

fn main() {
    println!("Benchmarks are temporarily disabled due to compilation issues");
}
